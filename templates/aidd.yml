version: 1

# Minimal config — all fields are optional with sensible defaults
# Project name auto-detects from directory name if omitted

# Uncomment and customize as needed:

# project:
#   name: my-project             # default: directory name

# agent:
#   model: sonnet                # opus | sonnet | haiku
#   max_parallel: 1              # concurrent agents
#   timeout_hours: 4             # kill agent after N hours

# commands:
#   test: ""                     # auto-detects: pytest, jest, vitest, go test, cargo test
#   lint: ""                     # e.g., "ruff check . --fix"

# prompt_instructions:
#   pre_implement: |
#     Before starting, read CLAUDE.md for project conventions.
#   post_implement: |
#     After all tests pass, verify the implementation is complete.

# paths:
#   specs: docs/specs/
#   plans: docs/plans/
#   worktrees: .worktrees/
#   state: .aidd/

# notifications:
#   telegram:
#     enabled: false             # configure ~/.aidd/.env first
